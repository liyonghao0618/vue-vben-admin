# AI API Service TODO

## 目标

为独立模块 `ai-api-service` 落地一个最小可用的诈骗识别 API 服务。

当前约束：

- 独立于主项目
- 先运行在 Codespaces
- 模型展示使用 `Qwen`
- 优先使用尽量小的模型
- 先解决“文本诈骗识别”这一条主链路

## 2026-04-19 本轮已完成

- [x] 完成独立 `Node.js + Fastify` 工程骨架
- [x] 完成诈骗识别主接口、批量接口、聊天记录分析、链接分析与人工复核接口
- [x] 完成本地规则兜底、提示词版本管理、审计日志、限流与鉴权预留
- [x] 完成 Docker / Docker Compose 部署文件与本地运行说明
- [x] 接通远程 Qwen 兼容接口配置链路
- [x] 修复远程兼容地址拼接问题，避免 `/compatible-mode/v1` 路径丢失
- [x] 增强 JSON 输出约束与解析能力，支持远程 Qwen 返回结构化结果
- [x] 验证远程模型实际生效，当前可返回 `provider: qwen`、`fallbackUsed: false`

## P0 初始化

- [x] 创建独立工程基础文件：`package.json`
- [x] 创建独立环境变量模板：`.env.example`
- [x] 创建独立忽略文件：`.gitignore`
- [x] 选择服务框架，优先考虑 `Fastify`
- [x] 规划 `src/` 基础目录结构
- [x] 增加启动脚本、开发脚本、构建脚本

## P0 最小可用 API

- [x] 实现 `GET /health` 健康检查接口
- [x] 实现 `POST /api/fraud-detect` 文本诈骗识别接口
- [x] 定义请求体结构，例如 `text`、`scene`、`source`
- [x] 定义响应体结构，例如 `isFraud`、`riskLevel`、`category`、`reason`
- [x] 增加统一错误响应格式
- [x] 增加基础请求参数校验

## P0 模型接入

- [x] 明确默认展示模型为 `Qwen`
- [x] 首选小模型方案，例如 `Qwen2.5-1.5B-Instruct`
- [x] 评估 Codespaces 是否能承受本地推理
- [x] 如果本地资源不足，预留远程模型调用适配层
- [x] 统一封装模型调用服务，避免业务层直接依赖具体模型实现
- [x] 固定输出为结构化 JSON，降低解析复杂度

## P0 诈骗识别能力

- [x] 设计诈骗识别提示词模板
- [x] 定义风险等级枚举：`low`、`medium`、`high`
- [x] 定义诈骗分类枚举
- [x] 支持输出命中原因和可疑证据片段
- [x] 支持输出用户处理建议
- [x] 为明显高风险场景增加规则兜底

建议优先覆盖的诈骗类别：

- [x] 冒充客服
- [x] 刷单返利
- [x] 虚假投资理财
- [x] 中奖或补贴诱导
- [x] 钓鱼链接
- [x] 验证码索取
- [x] 要求转账或下载陌生 App

## P1 工程完善

- [x] 增加日志能力
- [x] 增加请求追踪 ID
- [x] 增加基础限流
- [x] 增加鉴权方案预留
- [x] 增加批量识别接口 `POST /api/fraud-detect/batch`
- [x] 抽离提示词与规则配置

## P1 测试

- [x] 增加健康检查接口测试
- [x] 增加诈骗识别接口测试
- [x] 编写诈骗样例测试数据
- [x] 编写非诈骗样例测试数据
- [x] 增加模型输出非 JSON 时的兜底测试
- [x] 增加异常输入测试，例如空文本、超长文本

## P1 部署

- [x] 编写 `Dockerfile`
- [x] 增加本地运行说明
- [x] 增加环境变量说明
- [x] 增加部署说明
- [x] 评估模型文件下载、缓存和启动时间

## P2 后续扩展

- [x] 支持聊天记录批量分析
- [x] 支持链接风险辅助分析
- [x] 支持提示词版本管理
- [x] 支持规则引擎和模型联合判定
- [x] 支持审计日志和人工复核接口

## 当前建议

当前最值得优先完成的顺序：

1. 先建独立 Node.js 工程
2. 先做 `health` 和 `fraud-detect` 两个接口
3. 先接入 `Qwen` 小模型
4. 先跑通文本识别闭环
5. 再补测试、部署和批量能力

## 当前结论

当前 `ai-api-service` 已不再是占位模块，而是一个可运行、可测试、可接入远程 Qwen 的独立诈骗识别 API 服务。

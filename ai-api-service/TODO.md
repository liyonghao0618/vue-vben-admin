# AI API Service TODO

## 目标

为独立模块 `ai-api-service` 落地一个最小可用的诈骗识别 API 服务。

当前约束：

- 独立于主项目
- 先运行在 Codespaces
- 模型展示使用 `Qwen`
- 优先使用尽量小的模型
- 先解决“文本诈骗识别”这一条主链路

## P0 初始化

- [ ] 创建独立工程基础文件：`package.json`
- [ ] 创建独立环境变量模板：`.env.example`
- [ ] 创建独立忽略文件：`.gitignore`
- [ ] 选择服务框架，优先考虑 `Fastify`
- [ ] 规划 `src/` 基础目录结构
- [ ] 增加启动脚本、开发脚本、构建脚本

## P0 最小可用 API

- [ ] 实现 `GET /health` 健康检查接口
- [ ] 实现 `POST /api/fraud-detect` 文本诈骗识别接口
- [ ] 定义请求体结构，例如 `text`、`scene`、`source`
- [ ] 定义响应体结构，例如 `isFraud`、`riskLevel`、`category`、`reason`
- [ ] 增加统一错误响应格式
- [ ] 增加基础请求参数校验

## P0 模型接入

- [ ] 明确默认展示模型为 `Qwen`
- [ ] 首选小模型方案，例如 `Qwen2.5-1.5B-Instruct`
- [ ] 评估 Codespaces 是否能承受本地推理
- [ ] 如果本地资源不足，预留远程模型调用适配层
- [ ] 统一封装模型调用服务，避免业务层直接依赖具体模型实现
- [ ] 固定输出为结构化 JSON，降低解析复杂度

## P0 诈骗识别能力

- [ ] 设计诈骗识别提示词模板
- [ ] 定义风险等级枚举：`low`、`medium`、`high`
- [ ] 定义诈骗分类枚举
- [ ] 支持输出命中原因和可疑证据片段
- [ ] 支持输出用户处理建议
- [ ] 为明显高风险场景增加规则兜底

建议优先覆盖的诈骗类别：

- [ ] 冒充客服
- [ ] 刷单返利
- [ ] 虚假投资理财
- [ ] 中奖或补贴诱导
- [ ] 钓鱼链接
- [ ] 验证码索取
- [ ] 要求转账或下载陌生 App

## P1 工程完善

- [ ] 增加日志能力
- [ ] 增加请求追踪 ID
- [ ] 增加基础限流
- [ ] 增加鉴权方案预留
- [ ] 增加批量识别接口 `POST /api/fraud-detect/batch`
- [ ] 抽离提示词与规则配置

## P1 测试

- [ ] 增加健康检查接口测试
- [ ] 增加诈骗识别接口测试
- [ ] 编写诈骗样例测试数据
- [ ] 编写非诈骗样例测试数据
- [ ] 增加模型输出非 JSON 时的兜底测试
- [ ] 增加异常输入测试，例如空文本、超长文本

## P1 部署

- [ ] 编写 `Dockerfile`
- [ ] 增加本地运行说明
- [ ] 增加环境变量说明
- [ ] 增加部署说明
- [ ] 评估模型文件下载、缓存和启动时间

## P2 后续扩展

- [ ] 支持 OCR 后文本识别
- [ ] 支持聊天记录批量分析
- [ ] 支持链接风险辅助分析
- [ ] 支持提示词版本管理
- [ ] 支持规则引擎和模型联合判定
- [ ] 支持审计日志和人工复核接口

## 当前建议

当前最值得优先完成的顺序：

1. 先建独立 Node.js 工程
2. 先做 `health` 和 `fraud-detect` 两个接口
3. 先接入 `Qwen` 小模型
4. 先跑通文本识别闭环
5. 再补测试、部署和批量能力
